export default (firebase, uid, vaultId, updates) => {
  /**
   * Task 8: Update the vault
   * Subject: Firestore
   * Docs: https://firebase.google.com/docs/firestore/manage-data/add-data#update-data
   *
   * 1. Call `.update(updates)` on the user's `vault` doc.
   *
   * The `.update(updates)` function 
   */
  
   // TODO: Implement Task
};
